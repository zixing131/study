<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.study.kids.dao.ChineseCharacterMapper">

    <resultMap id="BaseMap" type="com.study.kids.entity.ChineseCharacter">
        <id property="id" column="id"/>
        <result property="charText" column="char_text"/>
        <result property="pinyin" column="pinyin"/>
        <result property="strokeOrder" column="stroke_order"/>
        <result property="words" column="words"/>
        <result property="sentence" column="sentence"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="findAll" resultMap="BaseMap">
        SELECT * FROM chinese_character ORDER BY sort_order ASC, id ASC
    </select>

    <select id="findById" resultMap="BaseMap">
        SELECT * FROM chinese_character WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO chinese_character (char_text, pinyin, stroke_order, words, sentence, sort_order)
        VALUES (#{charText}, #{pinyin}, #{strokeOrder}, #{words}, #{sentence}, #{sortOrder})
    </insert>

    <update id="update">
        UPDATE chinese_character
        SET char_text = #{charText},
            pinyin = #{pinyin},
            stroke_order = #{strokeOrder},
            words = #{words},
            sentence = #{sentence},
            sort_order = #{sortOrder}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM chinese_character WHERE id = #{id}
    </delete>
</mapper>
